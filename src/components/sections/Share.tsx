import classNames from 'classnames/bind'
import Section from '../shared/Section'
import styles from './Share.module.scss'

const cx = classNames.bind(styles)

function Share() {
  return <Section title="공유하기">공유</Section>
}

export default Share
